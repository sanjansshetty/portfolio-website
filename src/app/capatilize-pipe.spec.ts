import { CapatilizePipe } from './capatilize-pipe';

describe('CapatilizePipe', () => {
  it('create an instance', () => {
    const pipe = new CapatilizePipe();
    expect(pipe).toBeTruthy();
  });
});
